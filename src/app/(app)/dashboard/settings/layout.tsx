// Settings tab container. Placeholder for the real tab UI (ports in Step 4).
export default function SettingsLayout({ children }: { children: React.ReactNode }) {
  return <div data-section="dashboard-settings">{children}</div>;
}
