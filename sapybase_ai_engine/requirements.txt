fastapi
uvicorn
httpx
redis
psycopg2-binary
pydantic
python-dotenv
pgvector
# Pinned: response content shape varies across versions (2.5 returns a string,
# 3.x a list of blocks), so an unpinned rebuild could change what 22 .content
# call sites receive without a code change. Verified against the live API 4.3.3.
langchain-google-genai==4.3.3
langchain-core
langchain-community
langchain-text-splitters
svix
python-jose[cryptography]
# Direct dependency: BYOD DSN envelope encryption (byod_crypto.py, AES-256-GCM).
# Already pulled transitively by python-jose[cryptography]; declared explicitly
# since we now use it directly.
cryptography
requests
# Direct dependency: in-house URL extraction (services/html_extract.py).
beautifulsoup4
lxml
slowapi
clerk-backend-api
fastapi-cache2[redis]
pypdf
certifi
numpy
pybind11
wheel
gunicorn
uvicorn
jinja2
python-multipart
polar-sdk
pandas
# Direct dependency: Explore-plan billing anchor (usage_period.next_explore_billing_anchor).
# Already pulled transitively by pandas; declared explicitly since we now use it directly.
python-dateutil
openpyxl
alembic
# Observability — BYOD metrics emission (RFC §11 / Phase 8.4)
prometheus-client==0.21.1
